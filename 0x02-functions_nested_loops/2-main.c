#include "main.h"
#include <unistd.h>

/**
 * main - write the character c to store
 * c: the character to print
 *
 * Return: On success 1
*/
int main(void)
{
	char c;

	for (c = 'a'; c <= 'z'; c++);

	return (c);
}	
